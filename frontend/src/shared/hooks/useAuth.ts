/**
 * Hook useAuth
 * Export du hook d'authentification depuis AuthProvider
 */
export { useAuth } from '../providers/AuthProvider';

// Pour compatibilité avec l'ancien code
export type { AuthContextType } from '../providers/AuthProvider';

