services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: parabellum-frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - API_GATEWAY_URL=http://localhost:3001
    restart: always
