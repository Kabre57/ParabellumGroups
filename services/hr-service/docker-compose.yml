services:
  hr-service:
    build: .
    container_name: hr-service
    ports:
      - "4009:4009"
    environment:
      - PORT=4009
      - NODE_ENV=development
      - DB_USER=parabellum
      - DB_PASSWORD=parabellum2025
      - DATABASE_URL=postgresql://parabellum:parabellum2025@db:5432/parabellum_hr?schema=public
      - REDIS_URL=redis://redis:6379
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  db:
    image: postgres:15-alpine
    container_name: hr-service-db
    environment:
      POSTGRES_USER: parabellum
      POSTGRES_PASSWORD: parabellum2025
      POSTGRES_DB: parabellum_hr
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U parabellum -d parabellum_hr"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - hr-service_db_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: hr-service-redis
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  hr-service_db_data:
